# Environment / secrets (never commit the admin password hash)
.env

# SQLite database files (never commit collected leads / personal data)
storage/*.sqlite
storage/*.sqlite-wal
storage/*.sqlite-shm
storage/*.db

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
