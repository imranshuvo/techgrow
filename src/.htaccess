# Block all web access to the application source directory.
# Safety net for shared hosting where /src might sit inside the web root.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
